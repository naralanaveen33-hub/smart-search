# Runtime dependencies, pinned to the versions this build was verified against.
# A deploy months from now installs exactly what was tested, not whatever the
# latest release happens to be. Test-only packages live in requirements-dev.txt
# so they are never installed on the server.
#
# Requires Python >= 3.10 (the code uses `X | None` unions). See runtime.txt.

fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
httpx==0.28.1

# Supabase persistence (optional at runtime — the app falls back to local JSON
# storage when SUPABASE_URL / SUPABASE_SERVICE_KEY are unset).
supabase==2.31.0

# Document text extraction.
pypdf==6.15.0
python-docx==1.2.0
